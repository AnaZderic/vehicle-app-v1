"use client";

const Pagination = () => {
    return (
        <div>
            <button>left</button>
            <button>1...</button>
            <button>right</button>
        </div>
    );
};

export default Pagination; 